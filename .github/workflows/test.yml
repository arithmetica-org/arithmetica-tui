name: Test arithmetica-tui

on:
  push:
    branches: [ "main" ]

jobs:
  test-arithmetica-tui:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install nasm
      run: |
        sudo apt update
        sudo apt install nasm
    - name: Compile tests
      run: |
        cmake --no-warn-unused-cli -DBUILD_MAIN_EXECUTABLE=OFF -DARITHMETICA_TUI_TESTS=ON -DCMAKE_EXPORT_COMPILE_COMMANDS:BOOL=TRUE -DCMAKE_BUILD_TYPE:STRING=Release -S . -B build -G "Unix Makefiles"
        cmake --build build --config Release --target all -j 10 --
        chmod +x ./build/tests/arithmetica_tui_tests
    - name: Run tests
      run: ./build/tests/arithmetica_tui_tests